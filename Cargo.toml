[package]
name = "mm_ptr"
version = "0.1.0"
edition = "2021"

authors = ["lino <lino_snsalias@outlook.com>"]
description = "Smart pointers implementing abs_mm"
homepage = "https://github.com/ljsnogard/mm_ptr"
keywords = ["pointer", "alloc"]
categories = ["no-std"]
license = "MIT/Apache-2.0"
readme = "README.md"

[dependencies]
abs_mm = { version = "0.1" }
atomic_sync = { version = "0.1" }

[dev-dependencies]
core_malloc = { version = "0.1" }
log = { version = "0.4.*" }
env_logger = { version = "0.11.*" }